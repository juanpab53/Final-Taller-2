{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && corepack enable && pnpm install && pnpm run build"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma migrate deploy && node src/server.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
