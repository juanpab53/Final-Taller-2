{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install -g pnpm@11.5.0 && cd backend && pnpm install && pnpm run build"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma migrate deploy && node src/server.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
